const InfoText = () => {
    return (
        <div className="pt-3 text-white text-center font-bold">
            <div className="w-full">Pins should align with a line in the middle</div>
            <div className="w-full">
                Use <kbd>A</kbd> <kbd>D</kbd> <kbd>W</kbd> <kbd>S</kbd> to contol the pins. Press <kbd>SPACE</kbd> once finished
            </div>
        </div>
    );
};

export default InfoText;
